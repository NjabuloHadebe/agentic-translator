services:
  - type: web
    name: agentic-translator
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.api.main:app --bind 0.0.0.0:$PORT --workers 1
    envVars:
      - key: GROQ_API_KEY
        sync: false