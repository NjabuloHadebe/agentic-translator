
# === Core Framework ===
fastapi==0.104.1
uvicorn==0.32.0
gunicorn==21.2.0

# === Your Dependencies ===
groq==0.3.0
pydantic==2.12.5
python-dotenv==1.0.0
httpx==0.25.1

# === Vector DB & Embeddings ===
chromadb==0.4.24
sentence-transformers>=2.6.0

# === PyTorch (CPU) ===
# Keep these as lower-bounds so pip selects cp313 wheels on Render.
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# === LangChain ===
langchain>=0.1.0

# === Essential Dependencies ===
# Pin NumPy <2.0 to avoid ChromaDB's NumPy-2.x alias break (np.float_ etc.).
numpy>=1.26.4,<2.0
pandas>=2.1.3
scipy>=1.11.4
# Newer tiktoken publishes wheels; avoids Rust/maturin build.
tiktoken==0.12.0
aiofiles==23.2.1
python-multipart==0.0.6
# ChromaDB 0.4.24 requires orjson >= 3.9.12; keep <4 to avoid future breaking major.
orjson>=3.9.12,<4

# === Render-Specific ===
psycopg2-binary==2.9.9
``
